{
    "name": "distillery-trivia-root",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "build": "cd client && npm install --include=dev && npm run build && cd ../server && npm install --include=dev && npx prisma generate && npx prisma migrate deploy",
        "start": "cd server && npm start"
    }
}