{
    "name": "distillery-trivia-root",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "build": "cd client && npm install --include=dev --no-audit --no-fund && npm run build && cd ../server && npm install --include=dev --no-audit --no-fund && npx prisma generate && npx prisma db push --accept-data-loss",
        "start": "cd server && npm start"
    }
}